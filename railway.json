{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && gunicorn app:app",
    "buildCommand": "cd backend && pip install -r requirements.txt && apt-get update && apt-get install -y ffmpeg"
  }
}
